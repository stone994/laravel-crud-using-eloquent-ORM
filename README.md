# laravel-crud-using-eloquent-ORM
Laravel CRUD (Create, Read, Update, Delete) using Eloquent ORM is a way to interact with the database using models instead of raw SQL queries. Eloquent allows you to treat database tables as PHP objects, making your code more readable, structured, and easier to maintain.
